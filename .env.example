# Environment Variables Template for Railway Deployment
# Copy these exact values to Railway Dashboard → Your App Service → Variables

# Database Configuration (Railway will replace MySQL variables automatically)
DATABASE_URL=jdbc:mysql://${{MySQL.MYSQL_PRIVATE_URL}}/travelbuddy
DATABASE_USERNAME=${{MySQL.MYSQL_USER}}
DATABASE_PASSWORD=${{MySQL.MYSQL_PASSWORD}}

# Application Configuration
PORT=8080
SPRING_PROFILES_ACTIVE=prod

# Optional: Stripe Configuration (if using payment features)
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Optional: File Upload Configuration
# MAX_FILE_SIZE=10MB
# MAX_REQUEST_SIZE=10MB

# Optional: Logging Configuration
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_JFSD_TRAVEL=DEBUG

# Notes:
# 1. Do NOT change the ${{MySQL.MYSQL_PRIVATE_URL}} format - Railway uses this
# 2. The database name 'travelbuddy' will be created automatically
# 3. Copy these EXACTLY as shown (without quotes) to Railway Variables
# 4. Railway will automatically replace MySQL variables with actual values