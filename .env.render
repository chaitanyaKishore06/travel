# Environment Variables for Render Deployment
# Copy these to Render Dashboard → Your Web Service → Environment

# Database Configuration (Get this from your PostgreSQL service in Render)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# Application Configuration
PORT=8080
SPRING_PROFILES_ACTIVE=render

# Optional: Stripe Configuration
# STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Instructions:
# 1. Create PostgreSQL database in Render
# 2. Go to your database → Connect tab
# 3. Copy the "External Database URL"
# 4. Paste it as DATABASE_URL value above
# 5. Add these variables to your Web Service in Render